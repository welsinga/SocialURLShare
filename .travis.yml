language: android
jdk: oraclejdk7

android:
  components:
   - build-tools-19.1.0

branches:
  only:
    - master

notifications:
  email: false

before_install:

  # for gradle output style
  - export TERM=dumb

  # environment info
  - ./gradlew -v
  - uname -a

script: ./gradlew clean build